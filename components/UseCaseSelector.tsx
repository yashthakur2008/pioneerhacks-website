'use client';
import { useState } from 'react';
import { motion } from 'framer-motion';
import { useCases } from '@/data/content';
export function UseCaseSelector() { const [active, setActive] = useState(useCases[0]); return <section className="section-shell py-24"><div className="card grid gap-6 p-6 lg:grid-cols-[.45fr_1fr] lg:p-10"><div><p className="mono mb-4 text-xs uppercase tracking-[.28em] text-[#7668ff]">Use-case selector</p><div className="grid gap-2" role="tablist" aria-label="Use cases">{useCases.map((u) => <button key={u.id} role="tab" aria-selected={active.id===u.id} onClick={() => setActive(u)} className={`rounded-xl border px-4 py-3 text-left font-bold ${active.id===u.id?'border-white/30 bg-white/10':'border-white/10 text-[#9aa3b8]'}`}>{u.label}</button>)}</div></div><motion.div key={active.id} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="min-h-72 rounded-2xl border border-white/10 bg-[radial-gradient(circle_at_70%_20%,rgba(118,104,255,.22),transparent_20rem),#05060c] p-8"><h3 className="text-4xl font-black tracking-[-.05em]">{active.title}</h3><p className="mt-5 max-w-xl text-lg leading-8 text-[#b8bfd1]">{active.copy}</p><div className="mt-10 h-2 rounded-full bg-white/10"><motion.div className="h-full rounded-full bg-[#45a7ff]" initial={{width:'20%'}} animate={{width: active.id==='hackers'?'38%':active.id==='sponsors'?'68%':'92%'}} /></div></motion.div></div></section>; }
