import { caseStudies, testimonials, organizerGroups } from '@/data/content';
export function CaseStudies() { return <section id="proof" className="section-shell py-24"><p className="mono mb-4 text-xs uppercase tracking-[.28em] text-[#45a7ff]">Proof and people</p><h2 className="section-title mb-10 max-w-4xl font-black">Built for outcomes, organizers, and credibility.</h2><div className="grid gap-4 md:grid-cols-3">{caseStudies.map(([metric, label]) => <article className="card p-6" key={metric}><strong className="text-5xl font-black tracking-[-.06em]">{metric}</strong><p className="mt-4 text-[#9aa3b8]">{label}</p></article>)}</div><div id="team" className="mt-12 grid gap-5 md:grid-cols-2">{organizerGroups.map((g) => <article className="card p-6" key={g.club}><p className="mono text-xs uppercase tracking-[.24em] text-[#f4b62a]">{g.short}</p><h3 className="mt-3 text-2xl font-black">{g.club}</h3><ul className="mt-5 flex flex-wrap gap-2">{g.people.map((p) => <li className="rounded-full border border-white/10 px-3 py-1 text-sm text-[#d8def4]" key={p}>{p}</li>)}</ul></article>)}</div><div className="mt-12 grid gap-4 md:grid-cols-3">{testimonials.map(([who, quote]) => <blockquote className="card p-6" key={who}><p className="leading-7 text-[#d8def4]">“{quote}”</p><footer className="mono mt-5 text-xs uppercase tracking-[.18em] text-[#9aa3b8]">{who}</footer></blockquote>)}</div></section>; }
